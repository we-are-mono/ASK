# Backup files
*~

# Build artifacts
*.o
*.lo
*.la
*.a
*.so
*.so.*
*.ko
*.mod
*.mod.c
.libs/
.deps/
dist/

# Built binaries
cmm/src/cmm
cmm/src/cmm_rtnl_fuzzer
dpa_app/dpa_app

# Kbuild temporaries
.*.cmd
Module.symvers
modules.order

# Fetched sources (reproduced by make)
sources/

# Editor files
*.swp
*.swo

# Python bytecode
__pycache__/
*.pyc

# Local setup notes — environment-specific, not part of the codebase
IPSEC.md

# OpenEmbedded / kas build output (oe-logs/oe-workdir are symlinks into the
# bitbake work tree — no trailing slash so the symlinks match too)
oe-logs
oe-workdir
build/*.log
build/conf/
meta-ask/build/

# Claude Code / MCP tooling + local project instructions
.claude/
.playwright-mcp/
CLAUDE.md
